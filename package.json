{
  "name": "mockitt-platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"yarn dev:backend\" \"yarn dev:frontend\"",
    "dev:backend": "cd backend && yarn start:dev",
    "dev:frontend": "cd frontend && yarn dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
